import React from 'react';

const Home = () => {
  return (
    <div>
      This is the Home component
        <button onClick={() => console.log('clicked')}>Click</button>
    </div>
  );
}

export default Home;